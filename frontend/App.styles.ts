import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    backgroundStyle: {
        backgroundColor: 'black',
        width: '100%',
        height: '100%',
    },
    container: {
        paddingTop: 25,
        width: '100%',
        height: '100%',
        backgroundColor: 'black',
    },
});
